import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Gather Foods AI Voice Assistant',
  description: 'Voice-powered Indigenous Knowledge Assistant with advanced AI capabilities',
  keywords: ['AI', 'Voice Assistant', 'Indigenous Knowledge', 'Catering', 'Data Sovereignty'],
  authors: [{ name: 'Gather Foods AI' }],
  viewport: 'width=device-width, initial-scale=1',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <div className="min-h-screen bg-gradient-to-br from-slate-900 via-purple-900 to-slate-900">
          {children}
        </div>
      </body>
    </html>
  )
}
